# Phase 5 実装結果

## 添付最新版との統合

- 添付ZIPのGitHub Actions、`.gitignore`、Phase 2～4仕様書を保持
- 添付に存在しなかったPhase 5のブラウザー基盤、ダウンローダー、専用プレイヤー、テストを統合
- GitHub ActionsはYouTubeスクリプトテスト、Androidユニットテスト、Debug APK生成の順で実行

## 実装した内容

- YouTube広告対策を文書開始時に実行し、既知の広告メタデータをプレイヤー初期化前に除去
- 読込後は広告表示中のプレイヤーだけを対象に、スキップボタン操作と短い広告の終了処理を補助
- YouTube広告対策のON／OFFをプロフィール設定へ保存し、切替時は文書開始スクリプトも付け外し
- YouTubeのサイト別広告ブロックがOFFの場合は、文書開始スクリプトも無効化
- 複数タスク＋隣接起動による同一プロフィールの2窓起動
- 格納タブの左フリックで選択・展開し、選択タブ位置まで自動スクロール
- 格納タブの右フリックで閉じる
- 背景タブ、戻る／進むの修飾キーをCtrlへ統一
- PC／モバイル表示を上部と設定画面へ表示
- WebViewのピンチズームを有効化（PC表示を含む）
- ページ内動画検出、Download／Streamボタン、プロフィール別履歴、重複名 `(1)`、失敗時の再試行
- VOD m3u8の最高画質選択とMP4結合保存（DRM対象外）
- プロフィール別ストリーミング動画タイル、長押し複数選択削除
- 独立専用プレイヤー、1080p上限（それ未満は最高画質）、シークプレビュー、0.25～2倍速、ピンチズーム
- 専用プレイヤーでCtrl＋左右スライドによるフレーム単位の送り／戻し
- 背景再生と自動ピクチャーインピクチャーは既定OFF

## 検証

- YouTubeスクリプトのローカルシミュレーション: 11ケース成功
- GitHub ActionsでYouTubeスクリプトテスト後にユニットテストとDebug APKを生成
- 広告キーだけを除去し、本編の `videoDetails` を維持するケースを確認
- 広告状態でない動画、長時間動画、無限durationの動画をシークしないケースを確認
- この作業環境にはGradle実行ファイルと依存キャッシュがないため、Android APKのコンパイルと実機確認は未実施

## 制限

- YouTubeは配信側の変更が頻繁なため、Braveと同等の恒久的な完全遮断は保証できない
- Chrome／Firefox向け拡張機能をAndroid WebViewへそのまま導入することはできない
- DRM動画、ライブm3u8、暗号鍵を取得できない配信は保存対象外
- シークサムネイルは配信元がフレーム取得を拒否する場合、動画ポスターを代替表示する

## 完成前の確認

`docs/DEFERRED_BUGS.md` の2件をユーザーへ再提示し、別途まとめて修正する。
