# 完成前の不具合確認リスト

以下は今回の機能実装とは分け、ユーザーからまとめて不具合修正指示を受けた時点で対応する。

- Google／Yahooの検索結果からYouTubeへ遷移できない。リンクを押すと検索結果ページのPC／モバイル表示が切り替わるだけ。YouTubeの直リンクは開ける。
- Xのログインページで、アカウント名入力時に `a,[];'./` など一部キーが入力できないことがある。不正なキー入力後に正常なキーを一度押すと `ssss` のような連続入力になる。ペーストでログインした後は解消した。

リリース候補を完成扱いにする前に、このリストを必ずユーザーへ提示する。
