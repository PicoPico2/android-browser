# Androidブラウザ Phase 4 統合実装仕様

更新日: 2026-09-23
対象: Kotlin / Jetpack Compose / Android WebViewを使用する個人用Androidブラウザ
主な実機: Galaxy Tab S9 FE

## 1. 実装方針

- 既存の閲覧、プロフィール、タブ、広告ブロック機能を壊さない。
- 有料API、外部有料サービス、常設サーバーは使用しない。
- プロフィールごとのCookie・履歴・設定の分離を維持する。
- DRM解除、アクセス制限回避、保護された動画の抽出は実装しない。
- 機能を小さな単位に分け、各段階でビルドとテストを行う。
- 実装前に現在のリポジトリを基準にし、古いZIPへ差し戻さない。

## 2. 複窓

独自の2画面UIではなく、同じプロフィールのBrowserActivityをAndroidの隣接ウィンドウとしてもう1つ起動する。

- 設定に「隣に新しいウィンドウを開く」を追加する。
- Cookie、Webストレージ、プロフィール設定は同じプロフィール内で共有する。
- タブ一覧、選択タブ、閉じたタブ履歴はウィンドウIDごとに分離する。
- 片方のウィンドウを閉じても、同一プロセス内のもう片方を終了しない。
- 最後のBrowserActivityが終了した場合だけプロフィール用ブラウザプロセスを終了する。
- 異なるプロフィールの同時起動は今回の対象外とする。

## 3. タブ操作とメディア再生

### 3.1 格納中のタブ

- タップ: 一覧を展開せず、そのタブを即座に表示する。
- 左フリック: そのタブを表示・選択したうえでタブ一覧を展開する。
- 右フリック: タブ一覧を展開せず、そのタブを閉じる。
- 誤操作防止の移動量しきい値を設ける。
- 最後の1タブを閉じる場合は新しい空タブを作成する。

### 3.2 タブの再生制御

- 動画・音声を再生できるのは選択中のタブだけとする。
- 非選択タブでは再生中のvideo/audioを停止する。
- WebView全体、JavaScript、ネットワーク読み込みは止めず、準備・バッファリングは許可する。
- 再選択時は、非選択になる直前に再生していたメディアだけ再開を試みる。
- アプリ自体がバックグラウンドになった場合の動作と、タブ切替時の動作を分離する。

## 4. サイト別表示モード

Cookieではなく、URLのホストに基づいてWebViewのUser-Agentと表示設定を変更する。

- `youtube.com` と全サブドメインを既定でPC表示にする。
- `nicovideo.jp` と全サブドメインを既定でPC表示にする。
- `live.nicovideo.jp` も上記に含む。
- 手動の「このタブのPC／モバイル表示を切替」は残す。
- 手動変更はそのタブ内の上書きとして扱う。
- ニコニコのモバイル表示でのみ発生するプレイヤー表示不具合を、既定PC表示によって回避する。

## 5. 広告ブロック

### 5.1 基本方針

- 実サイトで広告が減っている現行フィルターを維持する。
- 添付評価ログの未遮断項目をすべて機械的に追加しない。
- 計測、端末メーカー、ログイン、決済などを広範囲に遮断してサイトを壊さない。
- ニコニコのモバイル表示不具合は広告ブロック由来として扱わない。

### 5.2 YouTube PC表示の広告対策

- モバイル表示向けの追加最適化は今回行わない。
- PC表示の広告状態として`ad-showing`と`ad-interrupting`を監視する。
- 新旧のスキップボタン候補を検出し、操作可能になった時点で押す。
- 1本目だけで停止せず、2本目以降の広告も継続して監視する。
- 監視間隔は250ms程度とする。
- 広告状態が明示され、長さが有限かつ広告として妥当な動画だけシーク対象にする。
- 本編動画を誤って飛ばさないことを最優先にする。
- YouTube側の表示タイミングにより、広告が一瞬表示される可能性は仕様上残る。

### 5.3 ページ上の残った広告を選択

広告ブロック設定に「ページ上の広告を選択」を追加する。

1. 現在のページを要素選択モードにする。
2. タップしたDOM要素を枠線で強調する。
3. 親要素方向への選択範囲拡大と、子要素方向への縮小を可能にする。
4. 非表示結果をプレビューする。
5. 適用するプロフィールを複数選択する。
6. 「選択したプロフィールでブロック」を押して登録する。

カスタムルールの要件:

- サイト単位のCSS非表示ルールとして保存する。
- カスタムルールごとに適用プロフィールIDの集合を持つ。
- 作成後も適用プロフィールを追加・解除できる。
- 選択されていないプロフィールには適用しない。
- 設定画面で、サイト別ルールの一覧、プレビュー、一時無効化、編集、削除を行える。
- 誤登録直後に「元に戻す」を表示する。
- `html`、`body`、本文全体など、ページを消す危険なセレクターは登録禁止にする。
- 選択面積が画面の大半を占める場合は警告する。
- ランダム文字列に見えるID・クラスを避け、安定した属性や親子関係を優先する。
- 初期版では通信遮断ルールを自動生成せず、要素非表示に限定する。
- クロスオリジンiframe、Canvas、動画映像内に描画された広告は選択対象外になり得る。

## 6. ダウンローダー

### 6.1 UIと履歴

- 左ツールバーのダウンロードアイコンからアプリ内ダウンローダーを開く。
- Androidのダウンロード一覧を開くだけの従来動作は置き換える。
- ダウンロード履歴はプロフィール別に保存する。
- 認証Cookieの値そのものは履歴データベースへ保存しない。
- 各項目にサムネイル、タイトル、再生時間、画質、ファイルサイズを表示する。
- m3u8などで事前に正確なサイズが得られない場合は「推定サイズ」と明記する。
- 各検出項目に`Download`と`Stream`の2ボタンを表示する。

### 6.2 通常ファイル

- HTTP/HTTPSの直接ダウンロードに対応する。
- Cookie、User-Agent、Refererを現在のプロフィールから引き継ぐ。
- 保存先は端末のDownload領域とする。
- 同名ファイルが存在する場合は上書きしない。
- `name.ext`、`name (1).ext`、`name (2).ext`の順に自動改名する。
- 待機中、進行中、一時停止、完了、失敗を表示する。
- 進捗、中止、失敗理由、再試行、完了ファイルを開く操作を提供する。
- 再試行では元の要求情報を再構築し、期限切れ認証情報を履歴から無理に再利用しない。

### 6.3 m3u8 VOD

- 初期版は終了点のあるVODのみ対象とする。
- Master Playlistを解析し、初期版では最高画質を自動選択する。
- 選択基準は帯域値を優先し、同等なら解像度が高いものを優先する。
- 将来、Download前に他の画質を手動選択できる構造にする。
- Media Playlistの動画・音声セグメントを順序どおり取得する。
- 音声が別Playlistの場合も取得してMP4へ多重化する。
- 通常取得できるAES-128鍵は対応可能範囲に含める。
- DRM、Widevine、ライセンス取得が必要な配信は対象外とする。
- ライブ録画、終了点のないPlaylist、アクセス制限回避は対象外とする。
- 成功後は一時セグメントを削除する。
- 中止・失敗時は安全に一時データを整理し、再試行に必要な非機密メタデータを残す。
- 空き容量不足、通信切断、URL期限切れ、結合失敗を区別して表示する。
- アプリ画面を閉じてもバックグラウンド処理を継続できるよう、Androidの制約に沿ったジョブとして実装する。

## 7. 保存前動画の専用プレイヤー

### 7.1 目的と起動

- 保存済み動画の再生を主目的にしない。
- Webページで検出した保存前の動画を、アプリ内でストリーミング再生する。
- `Stream`を押すとプロフィール別ストリーミング一覧へタイルを追加し、そのまま独立プレイヤーで開ける。
- プレイヤーはブラウザタブ内ではなく独立したActivityまたは独立画面として実装する。
- 起動時に元ページの動画を停止し、二重再生を防ぐ。
- プレイヤーを閉じた後、元ページを自動再生しない。

### 7.2 対応範囲

- 直接再生可能なHTTP/HTTPS動画。
- HLS/m3u8ストリーミング。
- Master Playlistの画質候補。
- DRM、Widevine、独自ライセンスが必要な配信は対象外。
- YouTubeなど、直接メディアURLの引き継ぎが安全・安定して行えないサービスは対象外になり得る。

### 7.3 再生UI

- 再生、一時停止、シーク、10秒戻る、30秒進む。
- 全画面、画面回転、音量、ミュート。
- 字幕トラックが取得できる場合の字幕表示。
- 音声トラック選択は実装しない。
- 連続再生は実装しない。
- リピートは実装しない。
- バックグラウンド再生は既定OFF。
- ピクチャーインピクチャーは既定OFF。
- 将来、両方をアプリ設定から変更できる設定項目と内部構造を用意する。

### 7.4 既定画質

- 1080pが利用できれば1080pを選ぶ。
- 最高画質が1080p未満なら、その動画の最高画質を選ぶ。
- 1080pがなく、1080pを超える候補だけの場合は1080pに最も近い候補を選ぶ。
- 解像度情報がない場合は帯域値を参考にする。
- プレイヤー内で利用可能な画質へ手動変更できるようにする。

### 7.5 シークサムネイル

- シーク操作中は必ず時刻とサムネイル領域を表示する。
- 配信にWebVTT、I-frame Playlist、画像スプライトなどのプレビューがあれば優先利用する。
- プレビューがない動画では取得済みデータからフレーム生成・キャッシュを試みる。
- 新しい位置の生成が間に合わない場合は、直近の取得済みサムネイルを時刻表示とともに使用する。
- サムネイル生成のために動画全体を事前ダウンロードしない。

### 7.6 再生速度バー

- YouTubeアプリに近い横方向の速度調整バーを設ける。
- 1.0倍を基準位置として分かりやすく表示する。
- 初期範囲は0.25倍から2.0倍とする。
- 代表値へスナップしつつ、バー操作で調整できるようにする。
- 選択速度はプロフィール別設定として保持可能にする。

## 8. ストリーミング動画タイル

- 左ツールバーのダウンロードアイコンの下に視聴アイコンを追加する。
- 視聴アイコンからプロフィール別のストリーミング動画一覧を開く。
- タイルにサムネイル、タイトル、再生時間、画質、追加元サイトを表示する。
- タイルをタップすると独立プレイヤーで再生する。
- タイル長押しで複数選択モードへ入る。
- 複数選択したタイルをまとめて削除できる。
- タイル削除は一覧情報と再生位置だけを削除し、保存ファイルや元ページには影響させない。
- 最後の再生位置はプロフィール別に保存する。

タイルへ保存する情報:

- メディアURL。
- 元ページURL。
- タイトル、サムネイル参照、再生時間、画質などの表示情報。
- User-AgentとReferer。
- 最後の再生位置。
- Cookie値そのものは保存しない。

期限切れURLへの対応:

1. 再生時に現在のプロフィールCookieを使用する。
2. URLが無効なら元ページから同じ動画の再検出を試みる。
3. 再取得できればタイルのURLを更新する。
4. 取得できなければ「元ページを開いて再検出」と表示する。

## 9. 拡張機能

- 今後追加する拡張機能本体・カタログは全プロフィール共通とする。
- 各拡張機能のON/OFF状態はプロフィール別に保存する。
- 新しい拡張機能を追加する際、全プロフィールへ無条件で有効化しない。
- 実行権限と対象サイトを将来表示できるデータ構造にする。

## 10. 実装順序

1. 複窓のライフサイクルとウィンドウ別タブ保存。
2. 格納タブのタップ・左右フリック。
3. 選択タブだけのメディア再生制御。
4. YouTube・ニコニコの既定PC表示。
5. YouTube PC表示の広告スキップ改善。
6. プロフィール選択式カスタム要素ブロック。
7. プロフィール別ダウンロード履歴と通常ファイル。
8. 動画検出UI、メタデータ、Download/Streamボタン。
9. ストリーミングタイル一覧。
10. 独立専用プレイヤー。
11. m3u8 VODのダウンロード、MP4結合、再試行。
12. プロフィール別拡張機能設定基盤。

各段階で既存機能の回帰テストを行い、巨大な一括変更にしない。

## 11. 受け入れ確認

- Galaxy Tabの分割画面で2窓が独立して操作できる。
- 一方を閉じても他方が終了しない。
- タブのタップ、左フリック、右フリックが仕様どおり動く。
- 非選択タブから動画・音声が流れない。
- YouTube PC表示で1本目と2本目以降の広告スキップ監視が続く。
- YouTube、ニコニコ、ニコ生が初期状態でPC表示になる。
- 選択した広告要素が、選択したプロフィールだけで非表示になる。
- 危険な要素選択を登録できず、ルールを後から無効化・削除できる。
- 通常ファイルで同名自動改名と失敗後の再試行が機能する。
- m3u8 VODが最高画質で取得され、再生可能なMP4へ結合される。
- Streamでタイルが作成され、独立プレイヤーから再生できる。
- 既定画質が1080p、または1080p未満の最高画質になる。
- シーク中に時刻とサムネイル領域が必ず表示される。
- タイルを長押しして複数選択・削除できる。
- バックグラウンド再生とピクチャーインピクチャーが既定OFFになっている。

## 12. 現時点の注意

- Phase 4の先行コードには、複窓、タブ操作、メディア停止、既定PC表示、YouTube広告監視改善、通常ダウンロード一覧、拡張機能設定の土台が含まれる。
- m3u8結合、独立専用プレイヤー、ストリーミングタイル、要素選択型カスタムルールは追加実装項目であり、先行コードで完成済みとは扱わない。
- 実装完了の判定にはAndroidビルド、単体テスト、Galaxy Tab実機テストが必要である。
